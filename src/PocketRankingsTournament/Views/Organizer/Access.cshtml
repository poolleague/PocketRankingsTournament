@{
    ViewData["Title"] = "Organizer access";
}
<section class="empty-page">
    <a class="back-link" href="/organizer">← Operations</a><p class="eyebrow">Owner only</p><h1>Organizer access</h1>
    <div class="empty-card"><h2>Account connection required</h2><p>The Tournament role model is active, but inviting real people requires the separate Pocket Rankings Account identity contract. Production fails closed until that work receives its own approval.</p></div>
</section>
